# Copy to `.env.local` and fill in for local development.
# `.env.local` is gitignored; this example file is committed.

# Deployed Google Apps Script web-app URL that records RSVP submissions
# (see server/apps-script/README.md). Leave empty for local-only behaviour:
# the form just shows the thank-you screen and sends nothing.
VITE_RSVP_ENDPOINT=
